div(layout="row" layout-margin)
    div(flex="40")
        | AdGroups Template

    .adGroup-list(flex="60")
        .adGroup(ng-repeat="adGroup in adGroupCtrl.adGroups")
            p AdGroup {{adGroup.name}}

include ./_footer.jade